spring:
  application:
    name: EventService

  datasource:
    url: jdbc:mysql://localhost:3306/event_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

server:
  port: 8081